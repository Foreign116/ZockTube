{
  "name": "zocktube-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "(cd client && npm install && npm install -g gatsby-cli && gatsby clean && gatsby build ) && (cd .. && npm install)",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "anime-scraper": "^3.1.1",
    "express": "^4.17.1",
    "path": "^0.12.7",
    "socket.io": "^2.5.0"
  }
}
